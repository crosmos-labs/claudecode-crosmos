---
description: Show Crosmos memory status — API key, resolved space, and connectivity.
allowed-tools: Bash(node:*)
---

Run and report the result to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/crosmos.cjs" status
```
